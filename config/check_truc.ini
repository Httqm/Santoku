[PATTERN]
define service {
    use                      generic-service
    host_name                $HOST_NAME$
    check_command            DO_TRUC
    service_description      $DESCRIPTION$
    _PARAM                   $PARAM$
    _WARN                    $WARN$
    _CRIT                    $CRIT$
    }


[VARIABLE2TAG]
# csvField => tag
host_name                  = $HOST_NAME$
description                = $DESCRIPTION$
param                      = $PARAM$
warn                       = $WARN$
crit                       = $CRIT$

